bot starten:
    im verzeichnis von der compose.yml -> docker compose up --build
    
bot stoppen:
    strg + c spammen wie ein depp

config (bot Token, Prefix, command name) kann man in der Dockerfile aendern
